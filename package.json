{
  "name": "potolok-calculator",
  "version": "1.0.0",
  "description": "Калькулятор для расчета стоимости натяжных потолков с Telegram ботом",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "calculator",
    "telegram-bot",
    "ceiling",
    "potolok",
    "pwa"
  ],
  "author": "andersen-123",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "node-telegram-bot-api": "^0.64.0",
    "dotenv": "^16.3.1"
  {
  "dependencies": {
    "express": "^4.19.2",
    "node-telegram-bot-api": "^0.66.0",
    "dotenv": "^16.4.5"
  }
}
```

3. Render автоматически переустановит пакеты

---

## 🎯 Что ждать дальше:

Следите за логами на Render. Через несколько секунд должно появиться:

### ✅ Если всё хорошо:
```
==> Starting service with 'npm start'

> potolok-calculator@1.0.0 start
> node server.js

✅ Сервер запущен
🌐 Веб-приложение: http://localhost:10000
🤖 Telegram бот активен
📅 Время запуска: ...
```

**Тогда идите в Telegram и проверяйте бота!** Отправьте `/start`

### ❌ Если ошибка:
```
❌ Ошибка: BOT_TOKEN не установлен в .env файле
```

**Значит забыли добавить переменную окружения:**
1. Во вкладке **Environment** на Render
2. Добавьте `BOT_TOKEN` со значением вашего токена
3. Нажмите **"Save Changes"**

---

## 📝 Напишите сюда:

Скопируйте следующие строки из логов Render (когда появятся):
```
==> Starting service with 'npm start'
...
