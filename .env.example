# Токен Telegram бота от @BotFather
BOT_TOKEN=your_bot_token_here

# Порт для веб-сервера
PORT=3000

# Режим работы (development/production)
NODE_ENV=development
```

4. **Commit new file**

---

## 🗑️ Шаг 4: Удалите старые файлы из корня

Теперь нужно удалить дубликаты:

1. Откройте старый `index.html` (в корне, не в папке public)
2. Нажмите на иконку корзины 🗑️ справа сверху
3. Внизу напишите: "Удалил старый index.html"
4. Нажмите **Commit changes**

Повторите для каждого файла:
- ❌ `style.css` (в корне)
- ❌ `script.js` (в корне)
- ❌ `manifest.json` (в корне)
- ❌ `service-worker.js` (в корне)
- ❌ `bot.js` (в корне)
- ❌ `bot-server.js`
- ❌ `README.txt`

---

## ✅ Шаг 5: Проверьте результат

После всех действий на главной странице репозитория должна быть такая структура:
```
📁 public/
  - index.html
  - style.css
  - script.js
  - manifest.json
  - service-worker.js
📁 bot/
  - bot.js
📄 .gitignore
📄 .env.example
📄 package.json
📄 server.js
📄 README.md
